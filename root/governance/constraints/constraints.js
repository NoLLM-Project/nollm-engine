// Constraint Map (inert)
// Holds inert constraint placeholders for each architectural layer.

export const Constraints = {
  root: {
    constraint: [
      "Inert placeholder for future root-level constraints",
      "Establishes that the root layer will eventually define constraint shapes"
    ],
    notes: [
      "Root constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  },

  system: {
    constraint: [
      "Inert placeholder for future system-level constraints",
      "Establishes that the system layer will eventually define constraint shapes"
    ],
    notes: [
      "System constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  },

  operational: {
    constraint: [
      "Inert placeholder for future operational-level constraints",
      "Establishes that the operational layer will eventually define constraint shapes"
    ],
    notes: [
      "Operational constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  },

  engine: {
    constraint: [
      "Inert placeholder for future engine-level constraints",
      "Establishes that the engine layer will eventually define constraint shapes"
    ],
    notes: [
      "Engine constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  },

  world: {
    constraint: [
      "Inert placeholder for future world-level constraints",
      "Establishes that the world layer will eventually define constraint shapes"
    ],
    notes: [
      "World constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  },

  assembly: {
    constraint: [
      "Inert placeholder for future assembly-level constraints",
      "Establishes that the assembly layer will eventually define constraint shapes"
    ],
    notes: [
      "Assembly constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  },

  architecture: {
    constraint: [
      "Inert placeholder for future architecture-level constraints",
      "Establishes that the architecture layer will eventually define foundational constraint shapes"
    ],
    notes: [
      "Architecture constraints at this stage are inert and non-behavioral",
      "They do not enforce or restrict anything",
      "They are fully reversible placeholders"
    ]
  }
};
