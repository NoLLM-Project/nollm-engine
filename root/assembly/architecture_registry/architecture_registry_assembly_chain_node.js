// Architecture Registry Assembly Chain Node (inert)
// Placeholder node for the registry assembly chain.

export const ArchitectureRegistryAssemblyChainNode = {
  step: null, 		// will eventually hold a chain step (inert)
  next: null, 		// will eventually point to the next chain node (inert)
};