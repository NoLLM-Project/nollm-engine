// Architecture Registry Assembly Chain Root (inert)
// Root placeholder for the registry assembly chain.

export const ArchitectureRegistryAssemblyChainRoot = {
  step: null, 		// will eventually hold the first chain step (inert)
  next: null, 		// will eventually point to the next chain node (inert)
};