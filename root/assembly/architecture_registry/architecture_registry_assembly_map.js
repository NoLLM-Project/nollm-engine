// Architecture Registry Assembly Map (inert)
// PLaceholder for mapping the registry assembly domain.

export const ArchitectureRegistryAssemblyMap = {
  domain: null 		// will eventually hold ArchitectureRegistryAssemblyDomain (inert)
  root: null 		// will eventually hold ArchitectureRegistryAssemblyRoot (inert)
  index: null 		// will eventually hold ArchitectureRegistryAssemblyIndex (inert)
};