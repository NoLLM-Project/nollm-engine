// Architecture Registry Assembly Chain Layer (inert)
// Placeholder layer for the registry assembly chain.
// Surface exposure (inert)

import * as ChainSurface from "./architecture_registry_assembly_chain_surface.js";

export const ArchitectureRegistryAssemblyChainLayer = {
  map: null, 		// will eventually hold ArchitectureRegistryAssemblyChainMap (inert)
  doamin: null, 	// will eventually hold ArchitectureRegistryAssemblyChainDomain (inert)
  root: null, 		// will eventually hold ArchitectureRegistryAssemblyChainRoot (inert)
  surface: ChainSurface, // inert exposure only
};