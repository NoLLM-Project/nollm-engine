{
  "id": "string",
  "object_id": "string",	// canonical name
  "allowed_parents": ["string"],
  "allowed_children": ["string"],
  "constraints": {
    "must_be_adjacent_to": ["string"],
    "cannot_be_adjacent_to": ["string"],
    "must_be_inside": ["string"],
    "cannot_be_inside": ["string"]
  },
  "invariants": ["string"]
}