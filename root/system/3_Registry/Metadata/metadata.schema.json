{
  "id": "string",		// metadata ID
  "name": "string",		// canonical name
  "sku": "string",		// SKU reference
  "coordinates": "string",	// coordinate reference
  "type": "string",		// object type
  "category": "string",		// semantic grouping
  "anchors": {			// continuity anchors
    "identity": "string",	
    "continuity": "string",
    "stability": "string",
  },
  "properties": {		// arbitrary metadata
    "key": "value"
  }
}
