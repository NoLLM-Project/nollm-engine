// Operational Registry Loader (inert)
// Placeholder for future registry population logic.

export function loadRegistry(manifest) {
  // Inert placeholder: does not wire or activate anything.
  return {
    world: manifest.world,
    engine: manifest.engine,
    semantics: manifest.semantics,
  };
}