# Registry metadata rules

- Metadata files may define descriptive attributes and types only.
- Metadata files must not define structure, geometry, or adjacency.
- Metadata files must not define behavior, logic, or scenes.
- Metadata files may reference IDs from identity, coordinates, and SKUs.