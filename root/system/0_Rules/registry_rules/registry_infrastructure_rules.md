# Registry infrastructure rules

- Registry infrastructure files may define loading, validation, and manifests only.
- Infrastructure files must not define domain architecture or behavior.
- Infrastructure files must not define geometry, routing logic, or UI structure.
- Infrastructure files may reference schemas and invariants from registry_rules.