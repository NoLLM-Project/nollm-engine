# Registry routing rules

- Routing files may define routing metadata: transitions, profiles, constraints.
- Routing files must not define architectural routing or adjacency.
- Routing files must not define behavior, logic, or concierge workflows.
- Routing files may only reference scenes, UI, and containers defined elsewhere.