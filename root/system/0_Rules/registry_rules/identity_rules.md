# Registry identity rules

- Registry identity includes: canonical names, aliases, SKUs, and ID mappings.
- Identity files must not define architecture, geometry, or adjacency.
- Identity files must not define behavior, logic, scenes, or UI structure.
- Identity files may only reference container types defined in system_invariants.