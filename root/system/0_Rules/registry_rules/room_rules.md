# Registry room rules

- Room registry files may define room IDs, mappings, and bindings only.
- Room files must not define room architecture, layout, or geometry.
- Room files must not define room behavior, logic, or semantics.
- Room files may reference identity, coordinates, metadata, and SKUs.