// Architecture Registry Container (inert)
// Structural placeholder for holding the registry system inside the architecture.

export const ArchitectureRegistryConstainer = {
  system: null, 	// will eventually hold RegistrySystem (inert)
  descriptor: null, 	// will eventually hold RegistrySystem descriptor (inert)
  slot: null, 		// will eventually hold ArchitectureRegistrySlot (inert)
  mount: null, 		// will eventually hold ArchitectureRegistryMount (inert)
};