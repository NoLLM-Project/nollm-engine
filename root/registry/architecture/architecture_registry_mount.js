// Architecture Registry Mount (inert)
// Structural placeholder for mounting the RegistrySystem descriptor.

export const ArchitectureRegistryMount = {
  descriptor: null, 	// will hold RegistrySystem (inert)
  slot: null, 		// will hold ArchitectureRegistrySlot (inert)
};