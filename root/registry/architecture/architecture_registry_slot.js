// Architecture Registry Slot (inert)
// Structural placeholder for receiving the registry system.

export const ArchitectureRegistrySlot = {
  system: null,  // will hold the RegistrySystem reference (inert)
};